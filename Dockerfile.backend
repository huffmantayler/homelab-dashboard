# Backend Dockerfile
FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./
# Install production dependencies
RUN npm ci --omit=dev

# Copy server code
COPY server.js .

# Expose port (default 3000)
EXPOSE 3000

# Start server
CMD ["node", "server.js"]
